import streamlit as st
from pathlib import Path

css_path = Path("assets/style.css")
if css_path.exists():
    st.markdown(f"<style>{css_path.read_text()}</style>", unsafe_allow_html=True)

st.markdown("<h1>How It Works</h1>", unsafe_allow_html=True)

st.markdown("""
1. **Upload PDF** → Drag your file into BrainBox.  
2. **Extract Text** → The PDF gets cracked open and the raw text is pulled out.  
3. **AI Summarizer** → The text is fed into an AI model trained to shrink big paragraphs into crisp notes.  
4. **Download** → You get clean notes you can download instantly.  

Simple. Fast. Student-friendly.
""")
